/** JPEG/WebP export quality when editing or re-encoding (0–1). */
export const UPLOAD_JPEG_QUALITY = 0.97;

/** next/image quality for feed cards and thumbnails (1–100). */
export const DISPLAY_QUALITY = 90;

/** next/image quality for full-screen viewers (1–100). */
export const DISPLAY_QUALITY_FULL = 100;
